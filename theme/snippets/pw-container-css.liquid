{%- comment -%}
  Primal Win — zoom-safe container (paste inside section {% style %})
  Requires pw-align-vars on same section root or parent.
{%- endcomment -%}
  width: 100%;
  max-width: var(--pw-max);
  margin-inline: auto;
  padding-inline: var(--pw-gutter);
  box-sizing: border-box;
